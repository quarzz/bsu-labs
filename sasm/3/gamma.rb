require_relative "constants.rb"

class Gamma
    attr_reader :function

    def initialize(shape = SHAPE, scale = SCALE)
        @shape = @shape
        @scale = @scale
        @function = lambda do |scale, shape|

        end
    end

    def next
    end
end
